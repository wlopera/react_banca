import React from "react";
import styles from "./Header.module.css";

const Header = () => {
  return (
    <header className={styles.header}>
      Proyecto Bancario -- wlopera @2024
    </header>
  );
};

export default Header;
